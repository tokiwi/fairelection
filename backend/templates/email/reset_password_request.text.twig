{% trans_default_domain 'emails' %}

** {{ email.subject }}
------------------------------------------------------------

{{ 'reset_password_request.paragraph0' | trans({}, 'emails', requestLocale) }}

{{ 'reset_password_request.link' | trans({}, 'emails', requestLocale) }} ({{ url }}/{{ token }})
